---
episode: "1.2"
title: "From noise to design"
duration: 15
level: Beginner
kind: Interactive concept
---

## Diffusion Playground

What changes between random noise and a recognizable architectural proposal?

The lecture describes a progression from noise toward structure. In the notebook, a text encoder represents the prompt and a denoising model iterates under that condition. Some pipelines work in a compressed latent representation rather than directly on display pixels.

:::genlab{kind=diffusion}
title: Diffusion Playground
question: What do you expect to happen to structure and detail as denoising progresses?
image: assets/generative/critic-library.png
alt: Fictional AI-generated library with a deep cantilever, broad stairs and glazed reading rooms.
pipeline: Noise → Text condition → Iterative denoising → Generated image
disclosure: "Conceptual visualization of iterative denoising: a fixed image is revealed through deterministic noise. These are not recorded intermediate or latent states from a diffusion model."
explanation: "Structure becomes easier to recognize as noise recedes. In a real diffusion pipeline, learned denoising updates are guided by the condition; the model does not simply fade an existing image into view. This slider illustrates the progression, not the algorithm or a quality score."
takeaway: Diffusion iteratively transforms noise toward structure under a condition; it does not draw the final image in one step.
:::

:::narration
Predict before moving the slider. What will emerge first: a building silhouette or small window details? The image here is fixed and the transition is a teaching simulation. A real diffusion model applies learned updates, often in a latent representation. The important idea is an iterative process steered by a condition, not a single act of drawing and not a lookup of a finished picture.
:::

:::reflect
Does moving this slider demonstrate that a real model will produce the same building at step 30?
- Yes, 30 steps always determines the image
- *No, this is an illustration of iteration, not an inference trace
:::

:::details{summary="Show reference answer"}
A real result also depends on the model, prompt, seed, scheduler, guidance and implementation. Thirty is an illustrative checkpoint count here, not a promise of quality or reproducibility across systems.
:::

## What controls an iterative process?

:::cards
Condition | What are we asking for? | A text prompt may describe a library, its setting, materials and light.
Seed | Where does sampling begin? | An initialization can lead to a different sample even under the same prompt.
Inference steps | How many updates? | More updates cost computation; quality gains can diminish and depend on the pipeline.
:::

A {{seed}} supports reproducibility only when the model and other relevant settings are also fixed. It is not a universal design identifier. A {{condition}} steers the process; it is not automatically a geometric constraint.

:::details{summary="Show reference answer · Why does a performance hint need checking?"}
“Passive cooling” may influence visible cues learned from images and captions. It does not supply airflow rates, constructions or a validated thermal model. Treat such cues as hypotheses for later evaluation.
:::
